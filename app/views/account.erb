<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <%if Helpers.is_logged_in?(session) == true%>
    <h1> Welcome <%= Helpers.current_user(session[:user_id]) %></h1> <br><br>
    <h1><%= session[:balance] %></h1> <br><br>
    <a href="/logout">Log Out</a>
    <%else%>
    <p> You Must <a href="/">Log In</a> to View Your Balance </p>
    <%end%>
  </body>
</html>
